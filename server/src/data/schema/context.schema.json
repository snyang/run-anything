{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "run anything server schema",
  "description": "run anything server schema",
  "type": "object",
  "properties": {
    "tags": {
      "description": "root: properties(tags[])",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "type": {
            "description": "property name(tags[].type)",
            "type": "string"
          },
          "value": {
            "description": "property name(tags[].value) -> property.name",
            "type": "string"
          }
        },
        "required": [
          "type",
          "value"
        ]
      }
    }
  },
  "required": [
    "properties"
  ]
}